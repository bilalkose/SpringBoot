package com.bilalkose.tutorials.utils;

public class ConfigProgramming {
    //DevTools => LiveReload
    //file -> settings -> compiler -> Build project automatically
    //settings -> advanced -> allow auto-make to start even
}
